name: Front Office Pipeline

on:
  push:
    branches: [main]
    paths:
      - "front-office/**"
  workflow_dispatch:

jobs:
  run-frontoffice:
    uses: ./front-office/.github/workflows/front-office-runner.yml